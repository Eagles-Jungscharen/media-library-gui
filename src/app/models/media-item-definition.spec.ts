import { MediaItemDefinition } from './media-item-definition';

describe('MediaItemDefinition', () => {
  it('should create an instance', () => {
    expect(new MediaItemDefinition()).toBeTruthy();
  });
});
